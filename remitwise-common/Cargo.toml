[package]
name = "remitwise-common"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# No std for WASM contracts
soroban-sdk = { version = "=21.7.7", default-features = false }

[dev-dependencies]
proptest = "1"

[lib]
crate-type = ["rlib"]

[features]
testutils = ["soroban-sdk/testutils"]
