name: Batch-B CI Gate
'on':
  pull_request:
    branches:
    - '**'
jobs:
  validate:
    name: Validate and Build
    runs-on: ubuntu-latest
    steps:
    - name: "Validate and Build (paused \u2014 CI stabilization in progress)"
      run: echo 'Validate and Build check intentionally stubbed while main-branch CI is being stabilized.'
